import React from 'react';

const Header = () => {
    return (
        <div className="chat-container">
            <h1>Test para la API de la FERIA de Muestras</h1>
        </div>
    );
};

export default Header;